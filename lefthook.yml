commit-msg:
  commands:
    commitlint:
      run: commitlint --edit {1}

pre-commit:
  commands:
    clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings
    fmt:
      glob: "*.rs"
      run: cargo fmt --check

pre-push:
  commands:
    test:
      run: cargo test
